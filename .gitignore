# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# 로컬 데이터 (모의/실전 DB, 로그)
data/
*.db
*.db-journal
logs/

# 자격증명 — keyring에 저장하며 파일로 남기지 않음
.env
*.secret

# 사용자별 엔진 설정. 추적하는 것은 settings.example.toml 뿐이다 —
# total_limit 은 사람마다 다르고, 남의 한도가 기본값처럼 딸려오면 위험하다.
settings.toml

# 세션 히스토리 (도구가 관리)
.remember/

# SDD 워크스페이스 (스크래치)
.superpowers/

# 커버리지 산출물
.coverage
htmlcov/
